diff --git a/manga_translator/config.py b/manga_translator/config.py
index 747b3e4..fe8294b 100644
--- a/manga_translator/config.py
+++ b/manga_translator/config.py
@@ -223,0 +224,8 @@ class TranslatorConfig(BaseModel):
+    api_base_url: str = 'https://api.deepseek.com'
+    """OpenAI-compatible API base URL supplied by the local client."""
+    api_key: str = ''
+    """OpenAI-compatible API key supplied by the local client."""
+    api_model: str = 'deepseek-v4-flash'
+    """OpenAI-compatible model name used for this request."""
+    disable_thinking: bool = True
+    """Whether to send the provider-specific thinking disabled parameter."""
diff --git a/manga_translator/translators/deepseek.py b/manga_translator/translators/deepseek.py
index d62b386..a91b4ba 100644
--- a/manga_translator/translators/deepseek.py
+++ b/manga_translator/translators/deepseek.py
@@ -56,3 +56,4 @@ class DeepseekTranslator(CommonGPTTranslator):
-        self.client = openai.AsyncOpenAI(api_key=openai.api_key or DEEPSEEK_API_KEY)
-        if not self.client.api_key and check_openai_key:
-            raise MissingAPIKeyException('DEEPSEEK_API_KEY environment variable required')
+        api_key = openai.api_key or DEEPSEEK_API_KEY or 'not-configured'
+        self.client = openai.AsyncOpenAI(api_key=api_key, base_url=DEEPSEEK_API_BASE)
+        self.model = DEEPSEEK_MODEL
+        self.disable_thinking = True
@@ -60 +61,0 @@ class DeepseekTranslator(CommonGPTTranslator):
-        self.client.base_url = DEEPSEEK_API_BASE
@@ -64,0 +64,7 @@ class DeepseekTranslator(CommonGPTTranslator):
+    def parse_args(self, args: TranslatorConfig):
+        super().parse_args(args)
+        api_key = args.api_key or DEEPSEEK_API_KEY or 'not-configured'
+        self.model = args.api_model or DEEPSEEK_MODEL
+        self.disable_thinking = args.disable_thinking
+        self.client = openai.AsyncOpenAI(api_key=api_key, base_url=args.api_base_url or DEEPSEEK_API_BASE)
+
@@ -245 +251 @@ class DeepseekTranslator(CommonGPTTranslator):
-            'model': DEEPSEEK_MODEL,
+            'model': self.model,
@@ -253,0 +260,2 @@ class DeepseekTranslator(CommonGPTTranslator):
+        if self.disable_thinking:
+            kwargs['extra_body'] = {'thinking': {'type': 'disabled'}}
diff --git a/manga_translator/translators/keys.py b/manga_translator/translators/keys.py
index c300a91..78fcbd1 100644
--- a/manga_translator/translators/keys.py
+++ b/manga_translator/translators/keys.py
@@ -39 +39 @@ DEEPSEEK_API_BASE  = os.getenv('DEEPSEEK_API_BASE', 'https://api.deepseek.com')
-DEEPSEEK_MODEL  = os.getenv('DEEPSEEK_MODEL', 'deepseek-chat') # Or: "deepseek-reasoner"
+DEEPSEEK_MODEL  = os.getenv('DEEPSEEK_MODEL', 'deepseek-v4-flash')
diff --git a/manga_translator/utils/inference.py b/manga_translator/utils/inference.py
index b0ee93e..6c0ee48 100644
--- a/manga_translator/utils/inference.py
+++ b/manga_translator/utils/inference.py
@@ -94 +94 @@ class ModelWrapper(ABC):
-    _MODEL_DIR = os.path.join(BASE_PATH, 'models')
+    _MODEL_DIR = os.getenv('MT_MODEL_DIR', os.path.join(BASE_PATH, 'models'))
diff --git a/server/sent_data_internal.py b/server/sent_data_internal.py
index a19096c..24cfc08 100644
--- a/server/sent_data_internal.py
+++ b/server/sent_data_internal.py
@@ -1 +0,0 @@
-import json
@@ -32,3 +31,3 @@ async def fetch_data(url, image: Image, config: Config, headers: Mapping[str, st
-                    return json.loads(await response.text())
-                except json.JSONDecodeError:
-                    raise HTTPException(502, detail='Invalid JSON response from upstream')
+                    return pickle.loads(await response.read())
+                except pickle.UnpicklingError as exc:
+                    raise HTTPException(502, detail='Invalid pickle response from upstream') from exc
